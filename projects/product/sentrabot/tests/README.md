# Tests routing

Executable tests for SentraBot live mainly under `apps/**` and `packages/**` and are run with `pnpm test` from the capsule root.

This `tests/` directory satisfies SAFRS capsule topology. Add capsule-level contract tests here when they are owned at the root rather than inside a package.

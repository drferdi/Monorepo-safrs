# Source placeholder

Product implementation for SentraBot lives under `apps/` and `packages/`, not here.

This `src/` directory exists so the capsule satisfies SAFRS repository topology (`tools/safrs/check_topology.py`). Do not add application code in this folder.

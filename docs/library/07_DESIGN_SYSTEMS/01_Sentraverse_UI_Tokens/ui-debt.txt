# Component-layer debt — selectors this repo still defines by hand.
#
# Every name below is a class that packages/sentra-ui already defines and an app
# redefines in its own stylesheet. Each one is a place where the app can drift
# from the system without any gate noticing, because a hand-built component made
# of semantic tokens passes check-tokens.mjs perfectly.
#
# This list may only shrink. `pnpm check:ui` fails on any collision that is not
# recorded here, so new drift is rejected immediately while the existing debt is
# migrated screen by screen. Remove a line when the app stops defining it and
# starts importing sentra-ui.css for it instead, then run
# `node scripts/check-ui.mjs --update`.
#
# Generated by scripts/check-ui.mjs --update. Do not add lines by hand to make a
# failure go away — that is the workaround UI-RULES.md warns about.

actions
alert
chips
chrome
chrome__path
chrome__right
filters
hide-md
hide-sm
icon
mark
mk-accent
mk-body
modal
num
pagehead
pagehead__id
pager
pager__info
pager__nav
search
seq
shell
skel
sr-only
state
status
summary
tablewrap
topbar
